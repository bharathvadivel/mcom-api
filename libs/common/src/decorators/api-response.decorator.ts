/**
 * API Response decorator for consistent response formatting
 */
export const ApiSuccessResponse = (options?: any) => {
  return (target: any, propertyKey?: string, descriptor?: PropertyDescriptor) => {
    // Implementation will be added when @nestjs/swagger is available
  };
};

export const ApiErrorResponse = (options?: any) => {
  return (target: any, propertyKey?: string, descriptor?: PropertyDescriptor) => {
    // Implementation will be added when @nestjs/swagger is available
  };
};

export const ApiPaginatedResponse = (model: any) => {
  return (target: any, propertyKey?: string, descriptor?: PropertyDescriptor) => {
    // Implementation will be added when @nestjs/swagger is available
  };
};